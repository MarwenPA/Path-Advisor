/** /admin/calendrier — Story 9.2 amendement 8.3 (CRUD visuel des jalons). */
import { CalendarAdmin } from "./calendar-admin";

export const metadata = { title: "Calendrier Parcoursup — Back-office" };

export default function AdminCalendrierPage() {
  return <CalendarAdmin />;
}
