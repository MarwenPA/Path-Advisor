/** /admin/modeles — Story 9.5 (versions du modèle IA, porte éthique). */
import { ModelVersions } from "./model-versions";

export const metadata = { title: "Modèles IA — Back-office" };

export default function AdminModelesPage() {
  return <ModelVersions />;
}
