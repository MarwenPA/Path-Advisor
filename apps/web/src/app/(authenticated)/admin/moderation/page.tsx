/** /admin/moderation — Story 9.4 (motivations 5.5 + commentaires écoles P2-5). */
import { ModerationQueues } from "./moderation-queues";

export const metadata = { title: "Modération — Back-office" };

export default function AdminModerationPage() {
  return <ModerationQueues />;
}
