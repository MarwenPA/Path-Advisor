/** /admin/signalements — Story 9.3 (file + SLA 7 j + actions). */
import { ReportsQueue } from "./reports-queue";

export const metadata = { title: "Signalements — Back-office" };

export default function AdminSignalementsPage() {
  return <ReportsQueue />;
}
